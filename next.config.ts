import type { NextConfig } from "next";

// ppr experimental feature could be used with the latest Next.js canary version
// npm install next@canary
const nextConfig: NextConfig = {
  //  experimental: {
  //   ppr: "incremental",
  //  }
};

export default nextConfig;
